version: 2
updates:
  # npm is left out on purpose: Expo pins compatible versions (`npx expo install --check`).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
